SELECT ID,
CASE
WHEN SIZE_OF_COLONY <= 100 THEN 'LOW'
WHEN SIZE_OF_COLONY <= 1000 THEN 'MEDIUM'
ELSE 'HIGH'
END AS SIZE
FROM ECOLI_DATA
ORDER BY ID ASC

CASE WHEN 문법을 사용해 컬럼을 새로 만드는 느낌으로 풀어야 한다.
파이썬이나 자바의 if문과 비슷한 느낌이다.
CASE부터 END까지가 하나의 컬럼이므로 그 뒤에 FROM이 붙는다.

할 때마다 새로운게 나와서 당황스럽다.
