SELECT A.ID, COUNT(B.ID) AS CHILD_COUNT
FROM ECOLI_DATA A
LEFT JOIN ECOLI_DATA B
ON A.ID = B.PARENT_ID
GROUP BY A.ID
ORDER BY A.ID ASC

처음 보는 유형의 SQL.
하나의 테이블을 두개처럼 생각해서 나눠야 하기 때문에 낯설다.
컬럼의 이름은 달라도 그 안에 들어있는 데이터의 의미가 같다면
언제든지 JOIN이 가능하다.
GROUP BY는 각 부모별로 자식들이 몇명인지 묶어서 집계하기 위해 쓴다.
