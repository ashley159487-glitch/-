def solution(num_list):
    answer = 0
    multiple = 1
    for i in num_list :
        multiple *= i
        answer += i
    if multiple < answer**2 :
        return 1
    else :
        return 0

for문은 반복을 할 뿐이다. 그 밑에 곱하기를 넣든, 더하기를 넣든 둘 다 넣든 상관없다.
주의점은 곱하기를 할 때는 변수에 0이 아닌 1을 넣는 것. 0 넣으면 값은 무조건 0이 된다.
