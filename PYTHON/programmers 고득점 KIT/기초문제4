1. n의 배수
def solution(num, n):
    if num % n == 0 :
        return 1
    return 0
배수는 나눴을 때 나머지가 0으로 떨어지는 것.

2. 공배수
def solution(number, n, m):
    if number % n == 0 and number % m == 0 :
        return 1
    return 0
처음엔 number % n * m == 0으로 했는데 이러면 문제가
파이썬에서 %(나머지)와 *(곱하기)는 우선순위가 같기 때문에
만약 number가 n의 배수라면 (number % n)은 0이 되므로, 여기에 m을 곱하면 m이 무엇이든 결과는 무조건 0이 되어버림.
n * m에 ()를 쳐도 마찬가지.

3. 홀짝에 따라 다른 값 반환하기
def solution(n):
    answer = 0
    if n % 2 == 1 :
        for i in range(1, n + 1) :
            if i % 2 == 1 : 
                answer += i
    else :
        for i in range(1, n + 1) :
            if i % 2 == 0 :
                answer += i * i
    return answer
맨 위의 if문은 n이 짝수인지 홀수인지 구분해주는 나침반.
이하면 무조건 n + 1을 해야 n까지 계산된다.

4. 조건 문자열
def solution(ineq, eq, n, m):
    if ineq == ">" and eq == "=" :
        if n >= m :
            return 1
        else :
            return 0
    elif ineq == ">" and eq == "!" :
        if n > m :
            return 1
        else :
            return 0
    elif ineq == "<" and eq == "=" :
        if n <= m :
            return 1
        else :
            return 0
    elif ineq == "<" and eq == "!" :
        if n < m :
            return 1
        else :
            return 0
문자열 두가지의 모양이 식 안의 비교식과 일치할 때 1을 리턴해야 하므로 그냥 모든 경우의 수를 구분해야하는 노가다를 했다.
나중엔 이걸 좀 더 가독성 좋고 짧게 바꿔봐야겠다.

5. flag에 따라 다른 값 반환하기
def solution(a, b, flag):
    answer = 0
    if flag == True :
        answer = a + b
    else :
        answer = a - b
    return answer
더 짧게 줄이면
def solution(a, b, flag):
    if flag:
        return a + b
    return a - b
이런식으로 True인지 굳이 비교 안해도 됨.
그리고 굳이 변수에다 값을 넣지말고 그냥 값 그대로 리턴해도 됨.
